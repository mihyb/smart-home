############################## openHAB Cloud Connector #############################

# The URL of the openHAB Cloud service to connect to.
# Optional, default is set to the service offered by the openHAB Foundation
# (https://myopenhab.org/)
#baseURL=

# Defines the mode in which you want to operate the connector.
# Possible values are:
# - notification: Only push notifications are enabled, no remote access is allowed.
# - remote: Push notifications and remote access are enabled.
# Optional, default is 'remote'.
#
# Was notification, which kept remote access closed but also made the dashboard
# unreachable: the connector still signs in, so myopenhab.org lists the instance
# as online, and then answers the dashboard with 502 Bad Gateway because there is
# nothing behind it to proxy. That reads like a broken tunnel rather than a
# setting, which is why it is written down here.
#
# remote reopens what notification was guarding: the myopenhab.org login now
# commands every item in the house, so that password is the key to the house and
# wants 2FA on the account. Push notifications work in either mode.
mode=remote

# A comma-separated list of items to be exposed to external services like IFTTT.
# Events of those items are pushed to the openHAB Cloud and commands received for
# these items from the openHAB Cloud service are accepted and sent to the local bus.
# Optional, default is an empty list.
#expose=
